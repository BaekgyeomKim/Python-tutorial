import numpy as np
import matplotlib.pyplot as plt

alpha_1 = np.arange(0,np.pi,np.pi/1000)
alpha_1_1 = np.arange(0,np.pi,np.pi/100)
alpha_2 = np.arange(2/3*np.pi,np.pi,np.pi/3000)

R = 25.
# print(R)
# print(type(R))

theta = 2.*np.arcsin(np.)
